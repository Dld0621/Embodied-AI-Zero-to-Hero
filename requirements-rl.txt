# RL dependencies — SAC, HER, Gymnasium-Robotics
# Install: pip install -r requirements-rl.txt
#
# Versions are pinned to match requirements-rl-lock.txt. Previously the loose
# constraints (gymnasium-robotics>=0.2.0, mujoco>=3.0.0) let pip resolve to an
# incompatible latest release:
#   - gymnasium-robotics==1.2.4 requires mujoco>=2.3.3,<3.0, so mujoco>=3.0.0
#     is incompatible (mujoco is therefore pinned to 2.3.7, the latest 2.x).
#   - the latest stable-baselines3 requires gymnasium>=1.0, which is
#     incompatible with gymnasium==0.29.1, so stable-baselines3 is pinned to
#     2.3.2 (gymnasium<0.30,>=0.28.1).
# numpy is pinned to 1.26.4 to match the lock file and avoid numpy 2.0
# runtime incompatibilities with stable-baselines3 2.3.x.

gymnasium==0.29.1
gymnasium-robotics==1.2.4
stable-baselines3==2.3.2
tensorboard==2.17.0
mujoco==2.3.7
numpy==1.26.4
