# Embodied AI Zero to Zero — Python Dependencies
# ============================================================
# This is the all-in-one requirements file.
# For granular installs, use the split files instead:
#   pip install -r requirements-core.txt   # numpy, scipy, matplotlib, etc.
#   pip install -r requirements-vla.txt    # torch, transformers, lerobot, peft
#   pip install -r requirements-wm.txt     # torch, numpy, matplotlib
#   pip install -r requirements-rl.txt     # gymnasium, stable-baselines3, tensorboard
# ============================================================

# --- Core ---
numpy>=1.23.0
scipy>=1.10.0
matplotlib>=3.7.0
pillow>=9.5.0
tqdm>=4.65.0

# --- Deep Learning (VLA + WM) ---
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0
einops>=0.7.0
timm>=0.9.0
tokenizers>=0.14.0

# --- VLA Fine-tuning ---
peft>=0.6.0
bitsandbytes>=0.41.0
wandb>=0.15.0
lerobot>=0.1.0

# --- Simulation & Robotics ---
mujoco>=3.0.0
opencv-python>=4.8.0
mediapipe>=0.10.0

# --- Reinforcement Learning ---
gymnasium>=0.29.0
gymnasium-robotics>=0.2.0
stable-baselines3>=2.2.0
tensorboard>=2.14.0

# --- Jupyter (optional, for tutorials) ---
jupyter>=1.0.0
